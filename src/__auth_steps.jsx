/**
 * 1. Create a new project on firebase ( console.firebase.google.com )
 * 2. npm install firebase
 * 3. create file name (firebase.init.js) and import getAuth to export auth
 * 3.5. const auth = getAuth(app);
 *  export default auth
 * 4. Firebase settings > Authentication > enable Emails and password auth etc...
 * 5. Create Login, SignUp components, setup route
 * 6. attach form field handler and form submit handler
 * 7. npm install --save react-firebase-hooks form ( react firebase hooks )
 * 8. createUserWithEmailAndPassword form ( react firebase hooks )
 * 9. if user is created redirect to the expected page
 * 10. useSignInWithEmailAndPassword for LogIn
 * 11. Create RequireAuth component ==> check user exists also tract user location
 * 12. In route wrap Protected Components by using Require Auth Components
 *
 */

/**
 * Firebase hosting steps
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 3. firebase init
 * 4. firebase deploy
 * 
*/
